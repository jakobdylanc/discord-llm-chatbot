# AGENTS.md

Agent guidance for this repository lives in [CLAUDE.md](CLAUDE.md). Read that file.

It is canonical for every agent CLI working here, not just Claude Code. This file is
a redirect on purpose: the guidance covers commands, module layering, the `on_message`
decision flow, and a set of gotchas that are wrong to keep in two places, because a
second copy drifts and then quietly contradicts the first.

Do not re-inflate this file.
